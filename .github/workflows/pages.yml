name: Manual Pages workflow note

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment source
        run: echo "This project page is intended to publish from main /docs via GitHub Pages branch publishing."
